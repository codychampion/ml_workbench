# Auto detect text files and perform LF normalization
* text=auto

# Force LF line endings for shell scripts (required for Docker containers)
*.sh text eol=lf
*.bash text eol=lf
Dockerfile* text eol=lf
